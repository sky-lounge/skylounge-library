---
uri: sky-lounge/skylounge-library/blueprints/cloudfoundry/bosh/blueprint.yml
params:
  main_branch: main
workflows:
  pull-request:
    jobs:
      dependency-review:
        - params:
          config_file: "sky-lounge/skylounge-library/jobs/github/security/dependency-review/config/cloud-gov.yml"
          external_repo_token: "${{ secrets.LIBRARY_TOKEN }}"
