---
steps:
  - name: Set up Java
    uses: actions/setup-java@v4
    with:
      distribution: ((distribution  ))
      java-version: ((jvm_version))
      cache: maven
  - name: Unit test
    run: mvn --batch-mode clean tests
