---
steps:
  - name: Set up Java
    uses: actions/setup-java@v3
    with:
      distribution: ((distribution))
      java-version: ((jvm_version))
      cache: "gradle"
  - name: Unit test
    run: ./gradlew test
